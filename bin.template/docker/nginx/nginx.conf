user  nginx;
worker_processes  2;

error_log  /var/log/nginx/error.log warn;
pid        /var/run/nginx.pid;


events {
    worker_connections  4096;
}


http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log  main;

    sendfile        on;
    #tcp_nopush     on;

    keepalive_timeout  120;

    #gzip  on;
    include /etc/nginx/conf.d/*.conf;
}

stream {
    # 负载均衡:权重
    # upstream yltcp {
    #     hash $remote_addr consistent;
   
    #     server smgate1:9091 weight=15 max_fails=10 fail_timeout=60s;
    #     server smgate2:9091 weight=5 max_fails=10 fail_timeout=60s;
    #     server smgate3:9091 weight=5 max_fails=10 fail_timeout=60s;
    # }

    # server {
    #     listen 9091 so_keepalive=on;
    #     proxy_pass  yltcp;
    # }

    # redisd代理
    # upstream daigou_redis {
    #     hash $remote_addr consistent;
    #     server 127.0.0.1:6379 weight=5 max_fails=1 fail_timeout=10s;
    # }

    # server {
    #     listen 8001;
    #     # proxy_connect_timeout 1s;
    #     # proxy_timeout 3s;
    #     proxy_pass daigou_redis;
    # }

    # # mysql代理
    # upstream daigou_mysql {
    #     hash $remote_addr consistent;
    #     server daigou_mysql:3306 weight=5 max_fails=1 fail_timeout=10s;
    # }

    # server {
    #     listen 8002;
    #     # proxy_connect_timeout 1s;
    #     # proxy_timeout 3s;
    #     proxy_pass daigou_mysql;
    # }
}